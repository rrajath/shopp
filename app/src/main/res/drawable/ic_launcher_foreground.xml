<?xml version="1.0" encoding="utf-8"?>
<!-- Not from the prototype (it has no icon asset; checked, none exists).
     Designed to match its own visual language instead: the app's most
     repeated motif is the circular checkbox and checkmark used on every
     row (ItemRow, Recently Completed), so the icon is that shape, enlarged. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FFD400"
        android:strokeWidth="6"
        android:pathData="M54,24 A30,30 0 1 1 53.9,24 Z" />
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FFD400"
        android:strokeWidth="7"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M39,55 L48,64 L72,38" />
</vector>
